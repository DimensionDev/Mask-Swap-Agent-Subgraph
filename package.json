{
  "name": "mask-swap-agent-mainnet",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ dimensiondev/mask-swap-agent-mainnet",
    "create-local": "graph create --node http://localhost:8020/ dimensiondev/mask-swap-agent-mainnet",
    "remove-local": "graph remove --node http://localhost:8020/ dimensiondev/mask-swap-agent-mainnet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dimensiondev/mask-swap-agent-mainnet"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
